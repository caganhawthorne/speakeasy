// Draws the current money total
draw_text(0,0,"$ "+string(global.money)+" / "+string(global.moneyneeded))
// Draws the current level
draw_text(0,20,"Level: "+string(global.level))

// TESTING

draw_text(0,60,string(alarm[0]))
